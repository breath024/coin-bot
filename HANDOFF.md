# 코인봇 인수인계서 (HANDOFF)

> 다음에 작업 시작할 때 **이 파일부터 읽으면** 바로 이어갈 수 있다.
> 최종 갱신: 2026-08-31 (날짜별 기록은 `logs/`)

## 0-I. 생성물 HTML은 커밋 금지 (2026-08-31)

`dashboard.html` · `grade_entries.html` 은 **돌린 결과물**인데 공개 저장소에 커밋돼 있었다.
안에 실매매 5건의 시각·진입가·청산가·수익률이 그대로 박혀 나온다.
gitignore 에 넣고 **이력에서도 지웠다**(커밋 3개 유지, 강제 푸시). 로컬 파일은 그대로다.

- 새로 만들어도 이제 커밋 안 된다. `git add -f` 로 억지로 올리지 말 것.
- 봇 코드·매매법은 이번에 안 건드렸다.
- 자세한 건 `logs/2026-08-31-log.md`.

## 0-H. 채점 자동 다음이동 (grade_entries.py 흐름 이식, 2026-07-10)
"채점할 때마다 표로 돌아가 다음 행 클릭하기 번거롭다" → 소스(봇/실거래/수동) 구분 없이 클릭 가능한
매매를 시간순으로 모은 `reviewQueue` 추가. 채점(1/2/3 또는 버튼) 하면 `grade_entries.py`와 똑같이
180ms 뒤 자동으로 큐의 다음 항목으로 넘어가서 바로 이어서 채점 가능. ←/→로 수동 이동도 가능(이전/다음
버튼 겸용). 리뷰박스 제목 옆에 "N / 총계" 진행률 표시. 지금 큐 13건(봇시드7+실거래5+수동1).

## 0-G. 진입/청산 핀 마커 + 차트 리사이즈 (2026-07-10)
- **핀 마커**: "가격라인만으론 어디서 진입/청산했는지 안 보여서 피드백하기 어렵다" → `drawCandles()`에
  `pins` 인자 추가(entry/exit 시각으로 가장 가까운 캔들을 찾아 그 위치에 점+수직선+라벨). 리뷰차트는
  전체폭 가격라인 대신 이 핀으로 교체(진입=주황 위쪽, 청산=파랑 아래쪽). 라이브차트도 `pos_state()`에
  `entry_time_ms` 추가해 열린 포지션의 진입 핀을 정확한 시각 위치에 찍음(시각 못 구하면 기존 가격라인
  폴백).
- **차트 리사이즈**: `.chartbox`에 `resize:vertical`(브라우저 기본 드래그 핸들) 추가, SVG는
  `ResizeObserver`로 크기 변화 감지해 캐시된 데이터로 재요청 없이 다시 그림(창 크기·회전 포함).

## 0-F. 채점 기능 + 차트 글자 뭉개짐 버그 수정 (2026-07-10)
- **채점 기능**: 리뷰차트(0-E)에 `grade_entries.py`와 같은 어휘(✓나도 들어감/?애매/✗난 안 침, 키보드 1/2/3)로
  채점 버튼 추가. `trade_grades.csv`에 기록, 재조회 시 표에 뱃지로 표시. `/api/grade` POST 엔드포인트.
- **버그: 맥북에서 차트 글자가 뭉개짐** — `preserveAspectRatio="none"` + 고정 `viewBox="0 0 900 260"`
  조합이 원인. 실제 렌더 폭(맥북 창 크기)이 900:260 비율과 다르면 SVG가 비균등 스케일링돼서 캔들뿐
  아니라 **텍스트까지 가로로 늘어나 뭉개짐**. `drawCandles()`가 매번 `svg.clientWidth/clientHeight`를
  읽어 viewBox를 실제 픽셀 크기로 맞추도록 수정(스케일 항상 1:1) + `preserveAspectRatio` 속성 제거 +
  창 크기 변경(resize) 시 캐시된 데이터로 재조회 없이 다시 그리기. (자산곡선 스파크라인은 텍스트가
  없어 기존 방식 유지, 문제 없음)
- **참고**: 라이브 봇거래 표가 비어있어(run_live 미가동) 데모용으로 `live_paper_log.csv`에 시뮬레이션
  트레이드 7건(진입+청산 쌍, 100x/risk1.5%, 6/30~7/9 로컬범위) 시딩해둠 — 사유란에 `[시뮬레이션 시드]`
  명시, 실제 라이브 거래 아님.

## 0-E. 과거 매매 리뷰차트 추가 (2026-07-10)
"시뮬레이션/과거 매매 뭐가 틀렸는지 볼 게 없다"는 지적 반영. 로그 표(봇/실거래/수동) **행을 클릭하면
그 시점 로컬 캔들 데이터(data/BTCUSDT_1m.csv)를 불러와 진입가/청산가 라인과 함께 화면 위 리뷰박스에
그려줌**. 신규: `build()`에 각 소스별 `trades`(진입/청산 epoch·가격) 병렬배열, `/api/history` 엔드포인트,
JS `drawCandles()` 공용화(라이브차트·리뷰차트 둘 다 재사용) + `reviewTrade()`/`reviewClick()`.
- **한계**: 로컬 캔들이 6/2~7/10만 있어서 그 밖 시점(예: 실거래 로그의 2026-01~02월 옛날 거래들)은
  "데이터 없음"으로 뜸 — 버그 아니라 백필 범위 한계. 이후 계속 쌓이면 자동으로 커버됨.
- 포트도 8790/0.0.0.0으로 이미 바꿔둔 상태(0-D)라 이 기능도 맥북 등에서 바로 씀.

## 0-D. 대시보드에 실시간 차트 + AI신호 배너 추가 (2026-07-10)
`report.py --serve`(버튼 모의매매) 점검하다 발견: **매매 버튼(사람 판단→기록)은 이미 되는데 정작
"차트"가 없었음** — 트레이드 패널에 현재가 숫자 하나만 뜨고 캔들/흐름을 볼 화면이 전혀 없어 실질적으로
맹목적 클릭이었음. 창업캠프에서 만난 팀장님이 "AI=보조신호, 실행은 사람"으로 쓴다는 얘기를 계기로
차트+신호+버튼을 한 화면에 합쳐 재구성:

- **차트**: `live_look.py`가 콘솔 텍스트로만 보여주던 흐름(MA5/MA20)·저항/지지(15분봉 20개 범위)·
  코일(수렴) 판정을 그대로 이식 — 신규 `chart_data()`(report.py) + `/api/chart`. 프론트는 순수 SVG로
  캔들 그림(외부 라이브러리 CDN 없음, 이 프로젝트 "표준 라이브러리만" 원칙 유지). 저항/지지 점선,
  현재가·진입가 라인 오버레이. 20초마다 갱신.
- **AI 신호 배너(팀장님 방식 반영)**: `live_signal()` + `/api/signal` — MomentumDip을 포지션 점유
  무관하게 스캔(`ml_dataset.scan_signals` 재사용)해 **"방금 마감된 봉에 진입신호가 있었는가"**만
  판정, 화면에 🔔배너로 표시. **봇이 대신 매매하는 게 아니라 사람이 보고 누를지 결정**(자동실행 X).
  신호가 뜰 때마다 `ai_signal_log.csv`에 시각·방향·가격·사유 기록(같은 봉 중복기록 방지) —
  나중에 `paper_trades_log.csv`(사람이 실제 누른 기록)와 시각 대조하면 **"신호 뜬 걸 사람이 실제로
  탔는가" / "신호가 사후적으로 맞았는가"** 둘 다 검증 가능한 원재료가 됨. **아직 정확도 집계 스크립트는
  없음** — 로그가 좀 쌓인 뒤 만들 것.
- ⚠ **포트 충돌 발견**: 기본 PORT=8765가 이미 다른(정체불명 — report.py 형식 아님, 아마 다른
  프로젝트) 서버가 물고 있음. `allow_reuse_address=True`라 Windows에서 새 프로세스도 조용히 같은
  포트에 bind는 되지만 실제 트래픽은 옛날 프로세스가 받아갈 수 있어 헷갈릴 수 있음. **`python report.py
  --serve` 돌렸는데 화면이 이상하면 그 옛날 프로세스부터 확인.** (호윤 미확인 프로세스라 손 안 댐)

## 0-C. ML 5모델 시도 → 표본부족 벽 재확인 (2026-07-10)
"머신러닝 가능할까" 실제로 테스트. `ml_dataset.py`(MomentumDip 신호를 포지션 점유 무관하게
전부 스캔=동시신호도 카운터팩추얼로 라벨링, 트레일규칙으로 개별 시뮬레이션) + `ml_models.py`
(시간순 학습/테스트 분할, 5모델). 데이터 data/ml_signals.csv.

- **표본 = 37.8일 전체에서 겨우 76건**(겹치는 신호까지 다 긁어도 이 정도 — consec 스윕이 그만큼 빡빡함, finding 6·8과 동형).
- 시간순 분할(학습54/테스트22)로 **LogReg·DTree(d3)·RandomForest·GBoost·KNN(5)** 학습:
  | 모델 | 학습acc | 테스트acc | 95%CI |
  |---|---|---|---|
  | LogReg | 66.7% | 36.4% | [16%,56%] |
  | DTree(d3) | 72.2% | 40.9% | [20%,61%] |
  | RandForest | 90.7% | 45.5% | [25%,66%] |
  | GBoost | 94.4% | 50.0% | [29%,71%] |
  | KNN(5) | 61.1% | 54.5% | [34%,75%] |
  테스트셋 베이스라인 승률 45.5%. **RF·GBoost는 학습90%대인데 테스트는 베이스라인 근처=전형적 과최적.**
  **모델이 고른 트레이드 손익도 전부 베이스라인(−64,093)보다 나쁘거나 비슷** — 5개 다 실전에서 못 씀.
  테스트 n=22면 신뢰구간 폭이 42%p라 모델간 차이는 우연과 구분 불가.
- **결론: 지금 데이터로 ML은 시기상조 — 섹션6 [4]의 "수백 건 이후" 전제가 실측으로 재확인됨.**
  76건 vs 필요한 수백 건, 거리가 멂. **표본부터(drill.py·실거래·run_live 누적)가 여전히 유일한 다음 수.**
  스크립트(ml_dataset.py/ml_models.py)는 재사용 가능하게 남겨둠 — 나중에 표본 쌓이면 그대로 재실행.

## 0-B. 횡보장 첫 검증 + trail 조기청산 원인 확정 (2026-07-10)
6/12 03:47 이후로 데이터 수집이 멈춰있어 바이낸스에서 백필(총 54,373봉, 최신 07-10 11:03).
새로 채워진 6/12~7/10(28일, 40,756봉) 구간은 63,545→63,731(**순변동 +0.3%, 사실상 횡보**,
단 구간 내 57,758~67,255 폭 15% 등락) = **지금까지 데이터가 다 하락장이었는데 처음 나온 횡보성 구간**
(0-2가 요구하던 다른 장세 검증용 데이터에 해당).

**① 기본 트레일 스윕(run_window.py)**: 전 레버 손실. 5x −0.9% / 10x −1.5% / 20x −1.5% / 100x −23.5%+청산1.
10x 33건 중 32건 소액(±수백)인데 7/6 22:43 숏 진입→7/7 00:51 −30%손절 한 건이 −15,228 = 6/9 패턴 반복(잔챙이 다수+큰손실 1방, finding 5-5와 동형).

**② trail vs oco A/B(compare_exit_mode.py, 같은 구간, 리스크 1.5% 고정) — ★0-A 결론 부분 반박:**
| | trail | oco rr1.0 | oco rr2.0 |
|---|---|---|---|
| 10x | −1.5% | −7.0% | −4.0% |
| 20x | −1.5% | −9.0% | −12.8% |
| 100x | −23.5% | −21.6% | −22.7% |
- **10x/20x는 trail이 oco를 이김 — 하락장 2구간(0-A)에서 "oco rr1.0 robust"라 봤던 결론과 반대.**
  oco는 포지션이 고정손절/익절 닿을 때까지 안 닫혀 재진입이 막힘(10x rr1.0 건수 8건 vs trail 33건) →
  횡보장에선 그 긴 보유가 되돌림에 그대로 노출돼 STOP 비율 급증(8건 중 6건 STOP).
- **0-A 자신이 적어둔 "두 구간 다 하락편향=단서지 증명 아님"이 실제로 맞았음.** oco rr1.0 채택 결정은 보류.

**③ ★ "왜 청산이 이렇게 자주 나오나" 원인 확정 (trail_pct=0.10%, strategy.py:31):**
10x trail 33건 보유시간 분석 — 19건이 진입 후 30분 이내, 25건이 1시간 이내 청산(평균 40분).
청산 사유 되돌림%가 거의 다 0.10~0.30%(문턱 0.10% 바로 위) — **1분봉 정상 노이즈 폭(0.1~0.3%)과
트레일 문턱이 겹쳐서, 조금만 유리하게 갔다가 살짝 흔들리면 바로 잘리는 구조.** 이게 손익비 0.55~0.72로
승자를 일찍 토하는 정체(finding 5-4, 0-A) 의 기계적 원인 — 재량 판단 문제가 아니라 **문턱 자체가 시장 노이즈
스케일에 안 맞음.**

**후보 수정 방향(★미적용, 결정 대기 — 실험 스크립트도 아직 안 만듦):**
1. trail_pct를 고정값 대신 **변동성 적응형**(최근 ATR/range 대비 비율)으로 — 조용한 구간엔 타이트, 출렁이는
   구간엔 넓게. 단 5-4가 "고정 트레일 폭 자체가 장세빨"이라 이미 반증한 바 있어 적응형도 재검증 필요.
2. 진입 후 **최소 유지시간/최소 봉수** 지난 뒤에만 트레일 발동(순수 노이즈성 즉시청산 차단).
3. **손익분기 스탑 후 트레일**: 일정 이익 나기 전엔 트레일 자체를 평가 안 함(버퍼), 이익 확보 후에만 되돌림 감시.
4. oco도 이번 데이터로 만능 아님이 드러남 → trail 완전 대체보다는 **위 1~3으로 trail을 손보는 쪽이 유력**.
- 어느 방향으로 검증할지, 검증 스크립트를 새로 짤지는 호윤 결정 대기.

## 0-A. 방향 재정렬 (2026-06-24, trading.txt 강의 대조)
호윤 1주차 강의(trading.txt) 핵심 = **그릇(리스크관리·멘탈) > 시스템(차트·타점)**. "시스템=변수(정답없음·과최적함정) / 리스크관리=상수(수학정답: 진입당 ≤5%)". 돈복사기(승률60%)도 40명중 2명만 벎=포지션사이징. 인간이 금붕어보다 못한 이유=감정(①손절미룸 ②본전조기탈출 ③연속손실후 베팅키움).
**→ 봇 자기결론(손익비 0.58=승자 일찍 토함, "봇가치=엑싯규율 대행")과 정확히 일치. 봇=금붕어(무감정 원칙집행기).** 그동안 노력이 *변수(진입튜닝)*에 쏠렸던 게 역전 → **진입 동결, 리스크·엑싯을 1급으로.**
- **리스크 1급화(broker.py `risk_per_trade`)**: 거래당 시드 리스크%를 직접 입력→증거금 역산(`margin=eq×risk/stop_on_margin`). 미설정 시 기존 margin_fraction 폴백(20개 분석스크립트 호환). `risk_per_trade_pct()`로 실효리스크 정직리포팅. 기존 실효리스크=margin_fraction0.05×stop0.30=**1.5% 고정**이었음(레버 무관)→이제 명시적.
- **금붕어 OCO(broker.py `rr` + strategy.py `exit_mode="oco"`)**: rr=손익비→익절폭=rr×손절폭 고정익절 예약. exit_mode="oco"면 재량 트레일링 OFF=진입시 건 손절·익절만 작동(감정개입 원천차단). 검증: run_backtest 종류별 {TP:8,STOP:4}=재량청산 0건.
- **run_backtest.py = 강의설정 데모**(risk 1%·rr 1.0·oco). run_live.py = 실효 1.5% 명시(데이터수집 행동 불변, 트레일 유지).

**엑싯 A/B (compare_exit_mode.py, 2종 실데이터 교차검증):** 같은 진입·같은 리스크(1.5%)·청산만 교체.
| | trail | oco rr1.0 | oco rr2.0 |
|---|---|---|---|
| 14d(-19.9%) 10x | +1.0% | +6.0% | +12.5% |
| 14d 100x | -12.1% | -7.2% | +0.8% |
| 13.6k(-9.4%) 10x | -0.0% | +6.9% | +2.2% |
| 13.6k 100x | -20.4% | -13.3% | -23.5% |
- **★robust(4/4): oco rr=1.0 > trail.** trail의 STALL(재량 조기청산 24~34건)이 승자 토해 손익비 0.55~0.72. = 강의 "본전 조기탈출"을 OCO가 기계적 차단.
- **★과최적함정: rr 2~3은 14d 하락장 압승이나 13.6k선 trail보다 나쁨 = 장세빨.** 高rr 박지 말 것. **기본 엑싯 = oco rr1.0**(대칭=금붕어 실험, 강의충실+robust). 단 두 구간 다 하락편향 = 단서지 증명 아님(상승/횡보 클린테스트 미존재).
- 100x는 oco로도 다 손실 = 5~10x 스위트스폿·수수료벽 재확인.

**④ 메타인지 로깅 완료 (R배수):** core.FuturesFill에 `r_multiple`(=실현손익÷계획리스크, 손절=-1R/익절=+rrR), broker._close서 계산, metrics._meta_cognition가 **기대값(R)·최악R·이상치손실(R<-1)·이상치비율** 산출. trade_log/run_live CSV에 R배수 열.
- **★핵심교훈: 손익비는 거짓말, 기대값(R)이 진짜 엣지.** trail 10x = 손익비 6.21(환상)인데 기대값 0.04R(찔끔이, R배수 다 +0.02~0.19) vs oco rr1.0 = 손익비 0.95인데 기대값 0.65R(±1R 대칭=돈복사기). R이 리스크정규화된 정직척도 = 강의 "승률·손익비 말고 기대값으로 시스템 보라".
- **이상치비율 = 레버 안전게이지.** 10x oco 0% vs 100x 0.33(손절이 수수료에 밀려 -1R 초과). 강의 #1죄(실효리스크>계획리스크) 자동적발.
- 미적용(다음): ⑤drill "잃기훈련 모드"(risk 1% 강제·비중키우면 실패처리). 진입은 계속 동결.

---

## 0. 한 줄 요약
호윤의 코인 무기한 선물 매매법(100x 격리, 훼이크 스윕 페이드)을 코드로 옮겨,
**돌아가는 모의매매 시스템 + 백테스트 + 대시보드**까지 1차 골격 완성.
**2026-06-25 방향 재정렬(↓0-A): 강의(trading.txt) 대조 → "진입(변수) 동결, 리스크·엑싯·메타인지(상수)를 1급으로."** 봇=금붕어(무감정 원칙집행). 리스크 1급화·금붕어 OCO·R배수 메타인지 구현·검증 완료.
이전 단계 = **데이터(=호윤 방향판단 라벨) 모으기**. **백테 파라미터 튜닝은 한계 도달**(swing≈ma·익절폭/SR필터 다 과최적, 5-5). 수익을 가르는 건 추세 끝물 역방향 한 방인데 그건 호윤 수동본능 영역이고 사례가 적어 검증불가. **→ 코드 병목 없음. 진짜 다음 수 = `drill.py`로 호윤 결정 표본 쌓기 + run_live/실거래.**

## 1. 빠른 시작 (자주 쓰는 명령)
```
python run_live.py        # ★ 내 매매법으로 라이브 모의매매 + 로그 (켜놓는 봇)
python live_look.py       # 지금 시장 한 컷 (흐름·저항/지지·코일)
python paper_trade.py short|long|status|close   # 손으로 모의매매 (CLI)
python import_trades.py   # 바이낸스 거래상세를 trades_raw.txt에 붙여넣고 실행 → 매매기록.csv
python run_realtest.py    # 실데이터 백테스트 (레버리지 스윕)
python diag_entry.py      # ★ 진입조건 깔때기 — 어디서 다 걸러지나 (과최적 감시용)
python near_miss.py [--tz 9]  # ★ '들어갈 자린데 안 들어간' 곳 KST로 뽑기 (코드 vs 내 감)
python tick_backtest.py [날짜] [--secs 60]  # ★ 틱 정밀 백테스트 (data.binance.vision)
python report.py          # dashboard.html 생성 (읽기전용, 더블클릭)
python report.py --serve  # ★ 대시보드 + 실시간 캔들차트 + AI신호 배너 + 진입/숏/청산 버튼 (localhost, CSV 기록됨)
python collector.py [once]# 원시 1분봉 적재 (선택; 원시시세는 재다운 가능)
python run_window.py [봉시각ms] # ★ '마지막에 본 봉' 이후~최신 구간만 모의투자 (cutoff 생략시 직전 세션 끝)
python drill.py [문제수]   # ★ 백데이터 결정 훈련기 — 미래 가리고 롱/숏/스킵 물음 → 채점+봇비교 → drill_log.csv 라벨 적재
python run_sr.py [cutoff]  # S/R 레벨 필터 검증(sr_filter 효과·6/9 거름여부). 결론=과최적, 기본 OFF
python compare_exit_mode.py [csv] [레버,..]  # ★ 엑싯 A/B: 같은진입·같은리스크, trail vs oco(rr별). 결론=oco rr1.0 robust(0-A)
```
※ 모두 표준 라이브러리만 사용, pip 설치 불필요. 바이낸스 공개 API(키 X).

## 2. 파일 지도
| 파일 | 역할 |
|---|---|
| core.py | 데이터 타입 (Bar, Side, FuturesPosition, FuturesFill) |
| account.py | 선물 계좌 — 격리마진·청산 (롱/숏) |
| broker.py | 가상 체결 + 청산/손절/익절 자동 (수수료·슬리피지) |
| **strategy.py** | **★ 내 매매법 (MomentumDip). 보정은 여기 ★ 파라미터** |
| engine.py | 백테스트 실행 루프 |
| feed.py | 합성 시세 (SyntheticFeed) / CSV |
| binance_feed.py | 바이낸스 실데이터·실시간 피드 |
| metrics.py | 성과·매매 통계 |
| trade_log.py | 백테스트 매매내역 CSV |
| run_backtest.py / run_sweep.py | 합성데이터 백테스트 / 레버리지 스윕 |
| run_realtest.py | **실데이터 레버리지 스윕** (최신 N봉 받아서) |
| run_window.py | **★ CSV에서 cutoff 이후 구간만** 레버리지 스윕 + 매매내역 (세션 사이 차트 검증용) |
| diag_entry.py | **진입조건 깔때기 진단** (funnel()/print_funnel() 재사용) |
| near_miss.py | **'안 들어간 자리' 진단** (KST, 공식룰 탈락 + 완화판 path 비교) |
| tick_backtest.py | **틱 정밀 백테스트** (data.binance.vision 덤프, data/ticks/ 캐시) |
| import_trades.py | 바이낸스 붙여넣기 → 매매기록.csv + 분석 |
| analyze_entries.py | 진입시각으로 차트 역추적 (눌림/MFE/MAE) |
| live_look.py | 현재 흐름·S/R·코일 한 컷 |
| run_live.py | **★ 실시간 모의매매 봇 → live_paper_log.csv** |
| paper_trade.py | 수동 모의매매 추적(CLI) → paper_trades_log.csv |
| report.py | 로그 → **dashboard.html** + `--serve` 시 **실시간 캔들차트(live_look 판정 이식)+AI신호 배너(0-D)+버튼 모의매매**(롱/숏/청산) |
| **drill.py** | **★ 백데이터 결정 훈련기** — 봇진입+통제 자리를 미래 가린 채 제시 → 롱/숏/스킵 입력 → 60분 실제+봇판단 공개·채점 → **drill_log.csv 라벨 적재**(=수익 못 푼 표본병목의 진짜 수집 수단) |
| run_sr.py | S/R 레벨 필터 검증(천장권롱/바닥권숏 거부). 결론=N=1 과최적, 채택X |
| **compare_exit_mode.py** | **★ 엑싯 A/B (trail vs 금붕어 OCO rr별, 리스크 고정). 결론=oco rr1.0 robust(단 0-B에서 횡보장 반례)** |
| **compare_min_hold.py** | **★ 0-B 후보②(최소유지시간) 다장세 검증. min_hold_bars 0/후보값 vs 일단위 윈도우 A/B** |
| **ml_dataset.py** | **0-C: MomentumDip 신호 전부(포지션 점유 무관) 스캔+트레일규칙 카운터팩추얼 라벨링 → data/ml_signals.csv** |
| **ml_models.py** | **0-C: ml_signals.csv로 5모델(LogReg/DTree/RF/GBoost/KNN) 시간순 학습·검증** |

데이터/로그: `data/BTCUSDT_1m.csv`, `data/ticks/`(틱 zip 캐시), `매매기록.csv`,
`paper_trades_log.csv`, `live_paper_log.csv`, `trades_raw.txt`(붙여넣기 원본)

## 3. 호윤 매매법 (코드에 담긴 규칙)
- **시장/방향**: 코인 선물, 롱/숏 둘 다 ("먹을 수 있는 자리는 일단 넣는다")
- **흐름**: 15분봉으로 추세 읽음 (코드: 1분봉 15개 묶어 15분 흐름 MA). 횡보면 안 침.
- **진입(핵심)**: 추세 반대 **훼이크 스파이크(청산사냥) ~0.4%** 발생 → 2~3봉 보며
  **힘 빠져 "거의 고정(삼각수렴: 고점↓+저점↑)"** 되는 순간 → 추세 방향 진입 → 대기(보통 5~7분 복귀)
- **이전 고점/저점(S/R) 레벨**에 걸칠 때를 더 중요시 (아직 코드 미반영 → TODO)
- **청산**: "올라갈 듯 지지부진하면 익절" = 트레일링(고저점 대비 되돌림). 승자 길게.
- **손절**: -30%(증거금). 청산(-50%)보다 먼저 닿게 설계. 실거래도 -33%에 끊음(규율 좋음).
- strategy.py ★ 파라미터: max_wait(2~3), sweep_pct(0.4%), flat_pct, trail_pct → **실거래로 보정 대상**

### 3-1. 코드 충실도 감사 (2026-06-03) — 코드 = 내 매매법의 "단순화된 v1"
골격(추세게이트→반대스파이크→힘빠짐→진입→트레일/손절)은 들어갔으나 **4곳이 다르거나 빠짐.**
이게 "코드와 내 감이 갈리는 보정 대상 목록" = 사례 쌓으며 하나씩 대조해 맞출 것.
| 내 규칙 | 코드 상태 |
|---|---|
| 추세 보고 진입 | ⚠️ **방식 다름**: 15분종가의 20MA(=사실상 **5시간 레짐**)+기울기. 내가 보는 15m 차트구조보다 훨씬 느림 → 내가 "추세"라 봐도 코드는 "횡보"로 스킵 가능(횡보 24%) |
| 훼이크 스파이크 0.4% | ⚠️ 깊이는 맞지만 **측정이 빡빡**(연속 신저점 갱신 누적 → 지그재그 눌림 못 봄. ↓finding 8) |
| 거의 고정(삼각수렴: 고점↓+저점↑) | ⚠️ **축약**: 봉 하나 범위≤0.15%만 봄. 2~3봉 수렴 *구조*가 아님 |
| 이전 봉 상한/하한(S/R) 걸칠 때 중시 | ❌ **완전 누락**. 코드가 보는 "이전봉"은 직전 1봉뿐. (3번 규칙·6장 TODO와 동일) |
| 롱/숏·max_wait·트레일링·-30%손절 | ✅ 그대로 (트레일은 0.1%로 빡빡) |
방향 주의: **S/R 추가 = 자리 걸러 품질↑(진입↓)** / **스파이크 path 재정의 = 놓친자리 잡아 수량↑(진입↑)** — 반대 방향.

## 4. 핵심 발견 (검증된 것)
1. **100배 = 죽음.** 합성·실15분·실1분 모든 백테스트에서 -90~99%+청산. 단 **선별력 강하면**
   (훼이크페이드만) 100배도 -7%로 안 터짐 → "선별 없는 100배"가 문제. **5~10배가 스위트스폿.**
2. **호윤 강점 = 진입/방향. 약점 = 청산 규율.** 실거래 손익비 0.58 = 승자를 너무 일찍/늦게 끊어 토함.
   라이브 모의매매에서도 피크 +31% → +14.2%로 토하는 거 직접 확인. **봇의 가치 = 엑싯 규율 대행(대체 X).**
3. **수수료가 조용한 살인자.** 100x 왕복 ≈ 증거금 8%. 자주 매매하면 출혈 누적.
4. **매매법 = 진짜 패턴.** 본인 설명 규칙을 코드에 넣으니 실데이터 승률 50→60→75% 상승,
   거래수 800→20(선별적). 역추적 MFE/MAE도 실거래와 일치. 운 아님(단 표본 작음).
5. **본인 말 vs 데이터 불일치**: "매수만" → 실제 숏도 함. "평균 35% 익절" → 실현 ~19~25%. → 로그 신뢰.
6. **진입 조건 깔때기 검증(diag_entry.py)**: 1분봉 1,514봉(25h)에 진입 9건. 로직 정합성 OK
   (관찰 재현 == 실제 진입). **병목은 딱 하나 = 스윕 깊이 ≥0.4%(④): 멈춤후보 194 → 11(5.7%)만 통과.**
   flat·max_wait는 거의 안 걸름(범인 아님). → **0.4% 깊이가 1분봉에선 거의 안 나옴.** 청산사냥 스윕은
   1분 안에 찔렀다 복귀해 1분봉이 못 봐서 ④에서 탈락하는 것일 수 있음 → **틱/1초봉 재생으로 ④ 통과가
   느는지가 핵심 검증 포인트(↓ 7번에서 검증됨).** (주의: 그 표본은 하락 56%편향 → 9건 중 8건 숏.
   25h로 엣지·임계값 판단 금지.) 파라미터 바꿀 때마다 `python diag_entry.py`로 깔때기 변화 확인 = 과최적 감시용.
7. **틱 검증 결과 = 6번 추측 반증(tick_backtest.py)**: 6/2 틱 274만개를 1분/10초/5초봉으로 재생해
   ④ 통과 비교 → **10 → 3 → 1건.** 고해상도일수록 ④가 **줄어듦**(예상과 반대). 이유: 전략이 스윕깊이를
   "연속 신저점 갱신 누적"으로 재는데, 봉을 잘게 쪼개면 노이즈로 그 연속이 끊겨 매 스파이크가 얕아짐.
   1분봉은 하락을 봉 하나 저가로 뭉쳐 깊이를 더 담음. → **현 로직에선 1분봉이 진짜 스위트스폿.**
   틱을 살리는 길은 둘: (a) 스윕을 "연속갱신"이 아니라 **시간창 내 최대낙폭(path)**으로 재정의 = 전략 변경,
   (b) **인트라바 체결 순서**(한 봉 안 손절/청산/익절 누가 먼저) = 봉 브로커 한계 해소(틱의 더 큰 값어치).
   둘 다 사례 더 쌓인 뒤 실험. 모든 해상도에서 깔때기 ⑤==실제진입 일치 → 스크립트 자체는 검증됨.
8. **'안 들어간 자리' 범인 확정 = 연속갱신(near_miss.py)**: 1분봉 1,514봉에서 공식 진입 **9건** vs
   완화판(연속 신저점갱신 요구 없이 창 안 최대낙폭=path로 측정)이면 잡았을 **'통째로 놓친' 자리 54건
   (path깊이 0.5~0.89%)**. 즉 0.5~0.9%짜리 명백한 눌림이 수두룩한데 코드가 "지그재그라 연속이 끊김"
   이유로 다 패스. 호윤 체감("이런거 많이 나오면 보통 들어가는데 아예 안 들어감")과 정확히 일치.
   → **범인 = strategy.py의 '매 봉 직전봉보다 신저점' 요구가 빡빡.** 고치는 방향 = 스윕을 path 기준 재정의
   (7-a와 동일). ※완화판은 진단용, 즉시 채택 X — 실거래 사례로 "나는 저 54개 중 어디 들어갔나" 대조 후 결정.
   (A: ③까지 갔다 떨군 185건은 대부분 0.29~0.40% '깊이부족' = 0.4% 문턱 살짝 아래)

## 5. 데이터 현황 (2026-06-05)
- 매매기록.csv: 실거래 5건 (승률 80%, 손익비 0.58) — **목표 30~50건**
- paper_trades_log.csv: 수동 모의 1건 (+14.2%)
- live_paper_log.csv: 봇 라이브 — run_live 켜야 쌓임
- data/BTCUSDT_1m.csv: 1분봉 **~4,420봉** (2026-06-02 16:51 ~ 06-05 18:32, 6/5 백필함)
- data/BTCUSDT_1m_14d.csv: 1분봉 **2만봉** (2026-05-24 ~ 06-07, 다장세 검증용 — `save_history.py`로 받음. 상승/횡보/하락 다 포함. 다음 검증은 이거 재사용)
- data/ticks/: 6/2 틱 zip 1일치 캐시(33MB, 274만틱) — tick_backtest 첫 실행분

### 5-1. 구간 백테스트 (run_window.py, 2026-06-05) — 직전 세션 끝(6/3 18:04)~6/5 18:32
**구간 = 48.5시간 -6.0% 하락장** (66,990→62,945). 봇이 진입한 10건 **전부 숏 = 방향 100% 적중.**
| 레버 | 수익률 | MDD | 승률 | 손익비 | 청산 | 건수 |
|---:|---:|---:|---:|---:|---:|---:|
| 5x | -0.1% | -0.8% | 66.7% | 8.02 | 0 | 9 |
| 10x | -0.2% | -1.5% | 66.7% | 8.01 | 0 | 9 |
| 20x | -2.1% | -3.2% | 64.3% | 0.28 | 0 | 14 |
| 100x | -3.1% | -8.3% | 60.0% | 1 | 1 | 15 |
- **결과 = 사실상 본전.** 10배 마감 9건은 6승3패·실현 +0.6%였으나 ①마지막 열린 숏(6/5 16:30 62,209)이 가격 반등으로 물려 평가손, ②수수료가 다 까먹음 → 순 -0.2%.
- **또 엑싯 병목.** 진입 스윕깊이 0.4~0.95%로 좋았는데 트레일 청산이 0.11~0.36% 되돌림에서 다 끊음 = 실거래 손익비 0.58 패턴 코드 재현. 하락장이라 큰 손실은 막아줌(저레버 청산 0)=양날검.
- 레버 교훈 재확인: 5~10x 본전방어, 100x -3.1%+청산1. **진입 엣지 OK, 병목은 여전히 엑싯 규율.**

### 5-2. 추세게이트 반증 + 본전 분해 (2026-06-05)
- **run_regime_sweep.py** — "봇이 6/5에 계속 숏친 건 추세판단이 느려서"라 보고 regime_ma를 20(5h)→3(45m)까지 줄여봄. **반증**: 빨라질수록 6/5 진입이 오히려 늘고(2→5), 6/5는 어느 설정서도 다 +. 2h로 줄이면 6/4 강추세를 -14k로 박살. → **MA *속도*는 범인 아님. 현 20이 이 구간 최선.** (단 5-3에서 '방식'은 범인으로 판명)
- **run_decompose.py** — 본전 범인이 레버별로 갈림. **5~10x: 마지막 미청산 숏**(실현만 +0.3~0.6%, 수수료 0.2~0.4%로 작음 → 포지션 해소되면 +일 듯). **100x: 수수료**(6.08% 토함, 빼면 +2.89%) = finding 3 "100x 왕복≈8%" 직접 재확인.

### 5-3. ★ 진입 채점 (grade_entries.py, 2026-06-05) — 봇 vs 나의 진짜 차이 = 방향결정
봇 10진입을 호윤이 차트로 직접 채점(✓들어감3 / ?애매5 / ✗안침2). **소름포인트: 봇이 잃거나 물린 3자리(#6 -113, #7 -286, #10 미청산)가 전부 호윤이 "롱 쳤어야/위험"이라 본 자리에 정확히 몰림.** 봇 깔끔히 번 #2·#3은 호윤도 "들어감". → **차이는 진입 자체가 아니라 방향(롱/숏) 결정. 호윤의 방향 재평가 = 봇 손실 필터 = 80% vs 본전의 정체.**
- **호윤 방향규칙(채점 메모에서 추출)**: ①"저점이 점점 높아지면 롱 / 고점 낮아지면 숏"을 **매 자리 swing 구조로 실시간 재평가**(#9·#10). 봇은 5h MA 관성이라 6/4후반~6/5 전환구간 못 따라가 헛발질. ②초록(상승)캔들 비율·큰 반등·이전 고가 미도달도 방향 근거(#1·#6). ③횡보면 안 침(#8). ④심리·S/R: "연속 같은방향 뒤 반대봉=전환임박", 이전 고저점 중시(#6).
- **청산도 어설픔 재확인**: #4·#8 "더 빨리/더 기다렸어야 — 계단형 하락 패턴 못 읽음".
- **결론**: 5-2에서 죽인 추세가설이 사실 반만 죽음 — *MA 속도*가 아니라 *추세 읽는 방식*이 문제. 호윤은 MA 아니라 swing(고저점) 구조로 봄. → **strategy.py에 swing 방향판정 추가**(trend_mode="swing", 아래).

### 5-4. ★ 트레일 익절 검증 → 진입 해부 (2026-06-06) = "익절이 아니라 진입이 문제"
손익비 0.58(승자 일찍 토함)을 "익절폭(trail_pct) 넓히면 되나"로 검증하다 더 깊은 결론에 도달.
- **run_trail_sweep.py** (전체 4422봉, 익절폭만 0.1%→1.0% 스윕): **100x에선 넓힐수록 손익비 0.76→3.82, 수익 -0.4%→+35%** (승률 64%→41% 트레이드오프). 1차로 "익절이 범인" 지지. **단 73h 하락 편향 = 단서지 증명 아님.** (10x는 손절이 가격상 3%로 멀어 0.7%+에선 토함 → 100x+넓은익절이 한 세트, 레버마다 최적 트레일 다름)
- **compare_trail_grade.py** (채점구간 10x, 0.1% vs 0.4% **자리별** 대조): **반증.** 0.4% 합계 +3,845 < 0.1% +6,114. 이 횡보성 구간은 빨리 챙기는 게 맞았고(#2·#5·#8은 먹은 걸 도로 토함), **추세 자리 #1만 길게가 +2,134 압승.** → **고정 트레일은 답 아님 = 과최적 함정 확인.** (장세 탄다)
- **anatomy_entries.py** (진입 10건 해부): **'길게 vs 짧게' 가르는 신호 = 직전 30봉 추세방향이 진입방향과 일치하는가.**
  - 추세순응 숏(#1·#3·#4, 직전추세 −0.4~−0.7%): MFE 1.0~1.5% / MAE 0.1~0.2% = **길게 간다**
  - 역행 숏(#2·#5·#7·#8·#10, 직전추세 +0.3~+1.3%): MFE 0.4~0.6% / MAE 1.2~2.0% = **금방 물린다**
  - → **5-3 채점("위험/롱쳤어야"라 본 자리)과 독립적으로 일치.** 호윤 감 = 머릿속 추세순응 필터. 5h MA 봇이 못 봐서 역행 숏을 침.
- **결론(방향 전환)**: 손익비 문제의 뿌리는 익절폭이 아니라 **진입에 추세순응(=방향) 필터가 없는 것.** 역행 숏을 거르면 남는 게 길게 가는 자리뿐 → 거기에 넓은 트레일 → **방향+익절이 한 방에 풀림.** (5-3의 swing 방향판정과 같은 결론에 다른 길로 도달.) 단 표본 10건·1구간 → 원리 검증 더 필요(과최적 경계).
- **새 스크립트 3개**: `run_trail_sweep.py`(익절폭 스윕), `compare_trail_grade.py`(자리별 0.1%vs0.4%), `anatomy_entries.py`(진입 MFE/MAE/추세 해부). 모두 진입로직 고정·익절/분석만.
- **1차 검증(compare_align.py + strategy.py에 `trend_align`/`align_lb=30` 옵션 추가, 기본 OFF)**: 직전 30봉 추세와 같은 방향만 진입(역행 거름). 6/2~6/5 한 구간에선 ON이 전체·채점 둘 다 수익↑·승률↑(채점 OFF −0.4%→ON +0.6%), 역행 숏이 거래의 절반(17→9, 9→3)이었음. **단 건수 3~9 소표본 + 같은 하락장 = 강한 단서지 증명 아님.**
- **다장세 재검증(validate_align_regimes.py, 최근 14일 2만봉, 일 단위 13윈도우) → ★반증.** 필터효과 거의 소멸: 하락 −0.14%(오히려 약간 손해)·횡보 +0.33%·상승 진입0건, **ON 나은 날 3 / 나쁜 날 3 / 동률 7 = 동전던지기.** → compare_align의 ON 압승은 **하락장 한 구간 통째로 본 착시.** **추세순응 필터(trend_align)는 하락장 우연일 가능성 → 채택 보류**(옵션은 코드에 남김, 기본 OFF). *교훈: 한 구간 결과를 일 단위·다장세로 펼쳐 재검증하는 습관 = 과최적 방패.*
- **★ 그 과정에서 드러난 진짜 병목 = 진입 빈도/표본.** 14일에 진입 ~20건, 거의 다 하락 5일에 쏠림, **상승장 0건.** 표본 이렇게 적고 한 장세 쏠리면 익절·필터 등 *어떤 튜닝도 통계검증 불가* = 그동안 20건 위에 모래성. finding 6·8(0.4% 스윕이 1분봉에 거의 안 나옴)과 직결. **→ 다음 우선순위 = 필터튜닝 아님. (a) near_miss의 path기준 스윕 재정의(finding 7-a·8, 진입수↑) 또는 (b) 라이브/실거래로 표본 모으기. 표본부터.**
- **이번 세션 새 스크립트**: run_trail_sweep / compare_trail_grade / anatomy_entries / compare_align / validate_align_regimes (모두 진입 타이밍 로직은 불변; 익절·방향필터·분석만).
- **path 스윕 이식(strategy.py `sweep_mode='path'`/`path_win=6` 옵션, 기본 consec) + compare_sweep_mode.py**: near_miss 완화판(창 내 최대낙폭, 연속갱신 불요)을 진입 정의로 이식. 14일 데이터서 **진입 23→60(2.6배) = 표본 병목 풀림(near_miss 예측대로).** 단 **품질 급락: 손익비 0.88→0.40, 수익 +1.1%→−2.0%, 평균손실 2배.** = consec의 '연속 갱신' 엄격함이 사실 품질필터였음. path 추가분 상당수가 가짜 눌림(추세지속→손절). **→ 양↑질↓ 트레이드오프, path 단독 채택 X.** 다음 = path로 양 확보 + **호윤이 path 진입을 grade_entries로 채점(진짜/가짜 라벨=호윤 강점)** → 그 패턴이 품질필터. (정렬 ON은 consec·path 둘 다 더 나쁨 = trend_align 보류 재확인)
- grade_entries.py에 모드 인자 추가: `python grade_entries.py path` → path 진입을 채점용 HTML로. (기본 consec 그대로)
- **path 디바운스(청산후 path_win봉 재진입 금지) 추가 후 재검증**: 본질 불변 — 14일 60진입 −2.0%·손익비0.40, 3일 31진입 −1.7%·0.41. **두 독립 데이터 모두 양↑질↓ 일관 → path 단독 채택 확정 X.** consec의 연속갱신 빡빡함이 진짜 품질필터였음.
- **★ 세션 메타결론(2026-06-06)**: 오늘 3실험(익절폭·추세정렬·path스윕) **모두 같은 벽 = "6월초 하락장 한 줌" 표본 한계.** 백테 위 파라미터/정의 튜닝은 수익체감 + 과최적 위험. **코드로 풀 단계는 거의 소진.** → 진짜 다음 수는 코딩 아님: **(A) `grade_entries.py path`의 49자리를 호윤이 채점해 path 후보 중 진짜만 거르기(=양 살리고 질 필터, 호윤 강점), (B) run_live/실거래로 진짜 표본 50건.** finding 우선순위 1(표본 모으기)이 결국 정답.

### 5-5. ★ swing 기본화 + 수익 연구 (2026-06-12) — "swing은 수익을 못 고친다"
6/5~6/12 데이터 동기화(13,617봉) 후 호윤 지시로 ① swing 기본값화 ② 수익 연구.
- **strategy.py: `trend_mode` 기본값 "ma"→"swing"으로 변경.** 단 *수익 근거 아님* — 코드를 호윤 실제 방식(저점높아짐=롱/고점낮아짐=숏)에 맞춘 것. 백테상 ma와 동률.
- **★ 깨끗한 A/B(run_swing.py 0, 같은 CSV 전체 9.5일, 10x): ma +0.11% vs swing −0.02% = 동률.** 둘 다 6/9 07:03 롱 −15,400을 똑같이 먹음. **직전(5-3 시절) "swing이 6/9 재앙 회피"는 윈도우 시작점 잘린 위치 덕분 = 요행.** 6/2부터 이어붙이면 swing 고저점 구조도 6/9를 상승으로 읽어 ma처럼 롱. → 과최적 함정 재확인. **swing은 방향개선 아님.**
- **★ 진짜 범인 = 6/9 롱 한 방.** 34~35건 중 나머지 다 ±수백~수천, 6/9만 −15,400(63,523 롱→16h→61,617 −30%손절, 직전고점 ~63,764 밑 끝물 롱). 그거 빼면 ma·swing 둘 다 +. 손익비 0.58의 순수형("잔챙이 다수+큰손실 1방").
- **익절폭 스윕(run_trail_sweep, swing, 전체 −9.4% 하락장): 10x 0.1%→1.0% 넓힐수록 −0.0%→+5.1%, 100x −20%→−3.5%.** 오르지만 **하락추세라 숏이 길게 간 덕 = 장세빨.** 5-4 compare_trail_grade(횡보장선 넓은트레일 토함)와 합치면 **고정 익절폭 robust 아님 = 과최적.** anatomy "방향필터+넓은익절 한방에 풀림" 가설은 swing이 방향개선이 아니므로 **미입증.**
- **결론(5-4 메타결론 재확인)**: 파라미터 튜닝으론 안 풀림(swing≈ma·익절폭 장세빨). **수익 가르는 단 하나 = 추세 끝물 역방향 한 방(6/9 롱). 이건 호윤 수동 본능("불안해서 안 들어감")이고 봇엔 없음.**
- **→ 다음 수 = S/R 레벨 필터(새 방법 아님, 3·3-1의 미구현 규칙).** 6/9는 직전고점 밑 끝물 롱 = S/R 규칙 있었으면 거부. 호윤이 "내가 안 들어갔을 자리"로 납득하면 strategy.py에 프로토타입→6/9 거르는지 검증. (untouchable 원칙: 진입변경은 본인 데이터 납득 후)
- **run_realtest(swing, 21일): 5x +0.6%·10x −0.3%·100x −13.2%.** ma판(5x +1.1%·10x +0.6%·100x −17.9%, 단 다운로드 시점 달라 비교 불완전)과 5~10x 동률~약간 아래·100x 나음. 5~10x 스위트스폿·100x 죽음 또 재확인.

**S/R 레벨 필터 프로토타입 + 검증 (2026-06-12, 호윤 "전자" 승인 후):** strategy.py에 `sr_filter`(기본 False)/`sr_lookback`(240)/`sr_room`(0.15) 추가 — "직전 sr_lookback분 범위의 천장권 롱·바닥권 숏 거부"(범위내 위치 pos>1−room이면 롱컷). 검증 스크립트 `run_sr.py`(swing·10x·전체 CSV, OFF vs ON 룩백×room 스윕, 6/9 롱 잡힘여부 출력).
- **★ 결과 = 과최적 함정, 채택 X.** 룩백 4h·12h·24h는 **모두 6/9 못 거르고 손익 악화**(−0.28~−0.61%). **딱 48h+room0.20~0.25에서만 6/9 거름 → +0.83%·손익비 5.33.** 그러나 그 개선은 **거의 전부 "그 한 트레이드(−15,438) 삭제" 효과**(35건 중 유일 대형손실 = N=1). 룩백×room 훑어 그 한 점 지우는 조합 찾은 것 = 사후맞춤. validate_align_regimes 교훈(하락장 한구간 착시) 재현.
- **★ 핵심: 6/9는 S/R 문제 아니라 레짐 반전.** swing 구조가 "저점높아짐=상승"이라 롱쳤는데 큰그림이 꺾임. 4h/12h/24h S/R에 안 잡힘 = 6/9는 단기 천장권 아니었음 = S/R 가설 반증. → **이게 호윤 수동본능("불안해서 안 들어감")이고, 그 실패유형 사례가 데이터에 1개뿐이라 어떤 필터도 검증 불가.**
- **권고: sr_filter 기본 OFF 유지(trend_align처럼 옵션만 코드에 남김).** 6/9형 사례 5~10개 쌓인 뒤 재검증. **백테 튜닝으론 안 풀림 재확인 → 진짜 다음 수 = 라이브/실거래로 "안 들어갔어야 할 자리" 표본 모으기.**

## 6. 다음 할 일 (우선순위)
0. **[열린 결정 → 0-B로 재오픈] run_live 엑싯을 oco rr1.0으로 전환할지** — 0-A(하락장 2구간)에선 trail보다 robust 우월이었으나, **0-B 횡보장 데이터에선 반대(10x/20x는 trail이 이김)**. oco rr1.0 채택은 보류, trail 유지 중. **호윤 결정 대기.**
0-1. **⑤ drill "잃기 훈련 모드"** — 강의 "1%씩 다 잃을 때까지" 훈련. 매 진입 risk 1% 강제, 비중 키우면 *실패* 처리. (0-A의 마지막 미구현 항목)
0-2. **[일부 완료] 횡보 데이터로 oco 재검증** — 0-B(6/12~7/10, 첫 횡보 구간)에서 완료, 결과=oco 우위 아님. 상승장 데이터는 아직 없음(누적 데이터가 다 하락~횡보) → 상승장 재검증은 여전히 미완.
0-3. **★[신규, 0-B] trail_pct(0.10%) 원인 확정 + 수정 방향 미결** — 1분봉 정상 노이즈(0.1~0.3%)와 트레일 문턱이 겹쳐 조기청산 다발(10x 33건 중 25건이 1시간 내 청산). 후보안 3개(변동성적응형/최소보유시간/손익분기후 트레일) 0-B에 적어둠, 검증 스크립트 미작성. **호윤 결정 대기 — 어느 방향으로 검증할지.**
1. **★ `python drill.py`로 결정 표본 쌓기 (지금 최우선)** → drill_log.csv에 호윤 방향판단 라벨 50~100건. 이게 SR필터·방향규칙을 검증할 유일한 표본(5-5 결론). 감 회복도 겸함.
2. **run_live.py 켜놓기** → 봇 라이브 로그 쌓기
3. **거래 로그 계속** (실거래 import_trades, 수동 paper_trade) → 30~50건 목표
4. **drill_log/거래로그 충분해지면** → "봇 스킵·내 진입" / "봇 진입·내 스킵" 패턴 분석 → 방향규칙/SR필터 재검증(과최적 경계). report.py로 봇 vs 나 비교.
5. 데이터 충분 → **[2] features.py(양자화) → [3] optimize.py(워크포워드 최적화)**
6. **[4] 학습**은 데이터 수백 건+ 후 XGBoost 등 단순모델부터. 딥러닝/LSTM 금물(과최적·데이터 함정).
   **(0-C에서 76건으로 5모델 실측 시도 → 전부 과최적/베이스라인 이하로 확인. 수백 건 될 때까지 재시도 보류.)**
7. **(틱 실험, 사례 쌓인 뒤)** finding 7 참고 — (a) 스윕을 시간창 내 최대낙폭(path)으로 재정의,
   (b) 인트라바 체결 순서(틱 단위 손절/청산/익절). 둘 다 전략·브로커 구조 변경이라 지금은 보류.
7. 폭락일 포함 검증 + 정밀검증용 틱데이터(data.binance.vision)

## 7. 열린 질문 / 확인 필요
- **바이낸스 거래시각 시간대?** (analyze_entries TZ 정렬용, KST 추정이나 UTC일 수도 — 본인도 불확실)
- run_live 세션 자산 초기화: 현재 매 실행 초기화(단순유지로 결정). 나중에 세션 구분 필요시 손보기.

## 8. 원칙 (잊지 말 것)
- **과최적(overfitting) 금지.** 백테스트 숫자 예쁘게 만들려고 파라미터 계속 돌리면,
  그 데이터에만 맞춰져서 실전에선 죽는다. 파라미터는 "수익률"이 아니라 "호윤 실제 행동"에 맞춰 보정.
- **검증 먼저, 시드 나중.** 10만원으로 ROI% 검증 → 증명되면 100/1000만 스케일업. 잃으면 안 되는 돈 X.
- **가상매매 손익 = 돈 아니라 데이터.**
- 진짜 정밀검증엔 **틱/1분 데이터** 필요 (100x 트리거가 봉보다 작음).

## 9. 맥락 (왜 시작했나)
2026-06-03 새벽, 알바 동료 전동윤과의 자동매매 협업이 깨진 일을 정리하다가
"내 매매법으로 내 봇을 만들자"로 전환. 호윤이 직접 돈 넣고 체득한 100x 선물 매매법이 출발점.
국장(주식봇)은 판 좁고 추적/세금 대비 실익 적어 코인 선물로 별도 프로젝트.
